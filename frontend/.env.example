# Local development: keep this blank so Vite proxies /api to Django on port 8000.
VITE_API_URL=

# For production / Capacitor builds, replace the blank value with your deployed API URL, e.g.
# VITE_API_URL=https://smart-expense-tracker-zaxw.onrender.com
